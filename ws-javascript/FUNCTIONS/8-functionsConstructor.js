/* 
  Function () constructor

  * expressão new
  * criar um novo objeto
  * this keyword
*/

let date = new Date("2020-12-01");

console.log(date);

// por isso do pascal-case
/* function Person(name) {
  // referência do jonhison
  // propriedade
  this.name = name;
  this.walk = function() {
    return this.name + " está andando";
  }
};

// modelo de objeto
// instânciar um objeto
const jonhison = new Person('Jonhison');
const joao = new Person('João');

console.log(jonhison.walk());
console.log(joao); */