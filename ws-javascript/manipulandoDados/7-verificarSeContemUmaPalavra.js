// Manipulando String

// Verificar se o texto contém a palavra Amor

let phrase = "eu quero viver o Amor!";
// verifica se tem uma determinada palavra -> includes
console.log(phrase.includes('Amor'));


// str[0].toUpperCase() + str.substr(1);
